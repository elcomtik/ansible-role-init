---
# defaults file for elcomtik.init

bootstrap_user: "root"
remove_bootstrap_user_ssh_keys: false

ssh_user: "ansible"
ssh_keys_path: "files/ssh-keys"
ssh_keys: []

ansible_port: 22
remove_default_ssh_port: false
